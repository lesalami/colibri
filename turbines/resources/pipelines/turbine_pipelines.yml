resources:
  pipelines:
    bronze_silver_gold_pipeline:
      name: turbines_etl
      serverless: true
      catalog: ${var.catalog}
      target: ${var.schema}
      configuration:
        input_path: ${var.input_path}
      libraries:
        - file:
            path: ../../src/turbines_etl/dlt_pipeline.py