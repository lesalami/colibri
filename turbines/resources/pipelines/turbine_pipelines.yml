resources:
  pipelines:
    bronze_silver_gold_pipeline:
      name: turbine-dlt
      development: true
      serverless: true
      catalog: ${var.catalog}
      schema: ${var.schema}
      libraries:
        - file:
            path: ../../src/turbines_etl/dlt_pipeline.py